name: Simple Mule blueprint
location: localhost
services:
- type: brooklyn.entity.mule.MuleServer
  name: Simple Mule blueprint
  period: 5000ms
  brooklyn.config:
    install.dir: ~/mule-home
    run.dir: ~/mule-home/mule-standalone-3.6.1
    version: 3.6.1
